// Classie placeholder - add the actual file content here
window.classie = window.classie || {
  add: function(el, className) { el.classList.add(className); },
  remove: function(el, className) { el.classList.remove(className); }
};